<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true">
        
        <!-- 声明这是一个 Kime 插件（通过 Intent Filter） -->
        <activity
            android:name=".PluginDeclaration"
            android:exported="true">
            <intent-filter>
                <action android:name="com.kingzcheung.kime.plugin.EXTENSION" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        
        <!-- 插件工厂类 -->
        <meta-data
            android:name="com.kingzcheung.kime.extension.factory.class"
            android:value="com.kingzcheung.kime.plugin.prediction.PredictionPluginFactory" />
        
    </application>

</manifest>